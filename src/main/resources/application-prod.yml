spring:
  application:
    name: school
  datasource:
    url: jdbc:postgresql://ep-ancient-violet-aocdyy53-pooler.c-2.ap-southeast-1.aws.neon.tech:5432/school_db?sslmode=require
    driver-class-name: org.postgresql.Driver
    username: neondb_owner
    password: npg_4crVDNUZvO6j
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 600000
      max-lifetime: 1800000
      auto-commit: true
  sql:
    init:
      mode: always
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

logging:
  level:
    root: INFO
    com.srs.school: INFO
    org.springframework.web: INFO

